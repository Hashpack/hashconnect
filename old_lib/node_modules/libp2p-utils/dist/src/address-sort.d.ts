export type Multiaddr = import('multiaddr').Multiaddr;
export type Address = {
    /**
     * peer multiaddr.
     */
    multiaddr: Multiaddr;
    /**
     * obtained from a signed peer record.
     */
    isCertified: boolean;
};
/**
 * Sort given addresses by putting public addresses first.
 * In case of equality, a certified address will come first.
 *
 * @param {Array<Address>} addresses
 * @returns {Array<Address>}
 */
export function publicAddressesFirst(addresses: Array<Address>): Array<Address>;
//# sourceMappingURL=address-sort.d.ts.map