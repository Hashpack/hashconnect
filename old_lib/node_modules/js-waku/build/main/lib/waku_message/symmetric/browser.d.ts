/// <reference types="node" />
declare global {
    interface Window {
        msCrypto?: Crypto;
    }
    interface Crypto {
        webkitSubtle?: SubtleCrypto;
    }
}
export declare function encrypt(iv: Buffer | Uint8Array, key: Buffer, clearText: Buffer): Promise<Buffer>;
export declare function decrypt(iv: Buffer, key: Buffer, cipherText: Buffer): Promise<Buffer>;
export declare function generateIv(): Uint8Array;
