import { shuffle } from 'libp2p-gossipsub/src/utils';
import { RelayCodecs } from './index';
/**
 * Given a topic, returns up to count peers subscribed to that topic
 * that pass an optional filter function
 *
 * @param {Gossipsub} router
 * @param {String} topic
 * @param {Number} count
 * @param {Function} [filter] a function to filter acceptable peers
 * @returns {Set<string>}
 *
 */
export function getRelayPeers(router, topic, count, filter) {
    if (filter === void 0) { filter = function () { return true; }; }
    var peersInTopic = router.topics.get(topic);
    if (!peersInTopic) {
        return new Set();
    }
    // Adds all peers using our protocol
    // that also pass the filter function
    var peers = [];
    peersInTopic.forEach(function (id) {
        var peerStreams = router.peers.get(id);
        if (!peerStreams) {
            return;
        }
        if (RelayCodecs.includes(peerStreams.protocol) && filter(id)) {
            peers.push(id);
        }
    });
    // Pseudo-randomly shuffles peers
    peers = shuffle(peers);
    if (count > 0 && peers.length > count) {
        peers = peers.slice(0, count);
    }
    return new Set(peers);
}
//# sourceMappingURL=get_relay_peers.js.map