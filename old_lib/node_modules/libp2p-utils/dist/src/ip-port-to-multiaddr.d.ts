export = ipPortToMultiaddr;
/**
 * Transform an IP, Port pair into a multiaddr
 *
 * @param {string} ip
 * @param {number|string} port
 */
declare function ipPortToMultiaddr(ip: string, port: number | string): Multiaddr;
declare namespace ipPortToMultiaddr {
    export { errors as Errors };
}
import { Multiaddr } from "multiaddr";
declare namespace errors {
    const ERR_INVALID_IP_PARAMETER: string;
    const ERR_INVALID_PORT_PARAMETER: string;
    const ERR_INVALID_IP: string;
}
//# sourceMappingURL=ip-port-to-multiaddr.d.ts.map